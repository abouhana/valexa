language: python
python:
  - "3.6"

install:
  - pip install pipenv
  - pipenv install --dev

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"

script:
  - pipenv run pytest --cov